@model IEnumerable<SOCIALCRUD3.Models.ReaccionesS>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPublicacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTypereact)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPublicacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTypereact)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdReaccion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdReaccion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdReaccion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
