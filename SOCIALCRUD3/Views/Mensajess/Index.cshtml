@model IEnumerable<SOCIALCRUD3.Models.MensajesS>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdEmisor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdReceptor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Texto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdEmisor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdReceptor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Texto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMsj">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMsj">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMsj">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
